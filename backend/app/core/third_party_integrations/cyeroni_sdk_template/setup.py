from setuptools import setup, find_packages

setup(
    name="cyeroni-sdk-template",
    version="0.1.0",
    packages=find_packages(include=['cyeroni_sdk_template*']),
    install_requires=[
        # Add your dependencies here
    ],
    python_requires=">=3.8",
)
